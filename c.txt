Since you **already have a working basic frontend UI** in the previous answer (index.html, recommend.html, dashboard.html, script.js, Bootstrap-ready layout), a *full new UI is **not strictly required*** for running your project.

✅ Therefore — **I will send the Deployment Guide** as you requested.

If later you want a **complete polished Bootstrap UI**, just say:
👉 **“Send full frontend UI”** and I’ll deliver it instantly.

---

# 🚀 **SmartCourse Deployment Guide (Beginner-Friendly + Production Ready)**

This guide covers **three deployment options**:

1. **Render (FREE – easiest, fastest)**
2. **PythonAnywhere (free, good for students)**
3. **VPS Deployment (Ubuntu, Nginx + Gunicorn) – professional production setup**

---

# OPTION 1 — 🌐 **Deploy Flask App on Render (FREE – Recommended)**

Render is simple and supports Python + Flask natively.

---

## ✅ Step 1 — Push Your Project to GitHub

Your repo should look like:

```
SmartCourse/
│── app.py
│── models/
│── data/
│── templates/
│── static/
│── requirements.txt
│── smartcourse.db
```

---

## ✅ Step 2 — Create `render.yaml` (optional, improves deployment)

Create this file at project root:

```yaml
services:
  - type: web
    name: smartcourse
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
```

---

## ✅ Step 3 — Deploy on Render

1. Go to: **[https://dashboard.render.com/](https://dashboard.render.com/)**
2. Click **New → Web Service**
3. Connect your GitHub repo
4. Set:

| Setting       | Value                             |
| ------------- | --------------------------------- |
| Runtime       | Python                            |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `gunicorn app:app`                |
| Region        | Any                               |
| Plan          | Free                              |

5. Click **Deploy**

Render installs everything and your app goes live in ~2–3 minutes.

---

# OPTION 2 — 📘 Deploy on PythonAnywhere (Beginner-Friendly)

PythonAnywhere free tier is good for university projects.

---

## ✅ Step 1 — Upload Files

1. Sign up at **[https://www.pythonanywhere.com/](https://www.pythonanywhere.com/)**
2. Open **Files** tab
3. Upload your entire project (`SmartCourse/`)

---

## ✅ Step 2 — Create Virtual Environment

Open **Consoles → Bash**:

```bash
python3.10 -m venv smartenv
source smartenv/bin/activate
pip install -r SmartCourse/requirements.txt
```

---

## ✅ Step 3 — Configure Web App

1. Go to **Web** tab → *Add a new web app*
2. Choose:

   * Framework → **Flask**
   * Python → **3.10**
3. Edit the WSGI file:

Replace this:

```python
from flask_app import app as application
```

With this:

```python
import sys
sys.path.append('/home/yourusername/SmartCourse')
from app import app as application
```

---

## ✅ Step 4 — Reload Web App

Your deployment is live.

---

# OPTION 3 — 🏢 Full Production Deployment (Ubuntu VPS, Nginx, Gunicorn)

This is the **professional method** (used for real-world apps).

---

## 🖥 Requirements

* Ubuntu 20.04 / 22.04 VPS
* SSH access
* Domain name (optional)

---

## Step 1 — Install Required Packages

```bash
sudo apt update
sudo apt install python3-pip python3-venv nginx git
```

---

## Step 2 — Clone Your Project

```bash
git clone https://github.com/yourname/SmartCourse.git
cd SmartCourse
```

---

## Step 3 — Create Virtual Environment

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

## Step 4 — Test Gunicorn

```bash
gunicorn app:app --bind 0.0.0.0:5000
```

(Stop with Ctrl+C)

---

## Step 5 — Create Gunicorn Service

Create:

```bash
sudo nano /etc/systemd/system/smartcourse.service
```

Paste:

```ini
[Unit]
Description=Gunicorn instance for SmartCourse
After=network.target

[Service]
User=ubuntu
Group=www-data
WorkingDirectory=/home/ubuntu/SmartCourse
Environment="PATH=/home/ubuntu/SmartCourse/venv/bin"
ExecStart=/home/ubuntu/SmartCourse/venv/bin/gunicorn --workers 3 --bind unix:smartcourse.sock app:app

[Install]
WantedBy=multi-user.target
```

Start service:

```bash
sudo systemctl start smartcourse
sudo systemctl enable smartcourse
```

---

## Step 6 — Configure Nginx Reverse Proxy

```bash
sudo nano /etc/nginx/sites-available/smartcourse
```

Paste:

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://unix:/home/ubuntu/SmartCourse/smartcourse.sock;
    }
}
```

Activate:

```bash
sudo ln -s /etc/nginx/sites-available/smartcourse /etc/nginx/sites-enabled
sudo nginx -t
sudo systemctl restart nginx
```

Your site is now live.

---

# 🟢 BONUS: Optional Improvements After Deployment

If you want, I can also provide:

✔ HTTPS setup (Let’s Encrypt SSL)
✔ Database migration (PostgreSQL / MySQL)
✔ Auto-reload on push (GitHub Webhooks)
✔ Logging system
✔ Docker deployment
✔ CI/CD pipeline

Just say **“Send deployment upgrades”**.

---

# ✅ DONE!

Your SmartCourse system is now deployable anywhere.

If you want the **full Bootstrap polished UI**, just say:
👉 **“Send full frontend UI”**
